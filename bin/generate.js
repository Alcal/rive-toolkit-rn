/**
 * Generate script – placeholder implementation.
 * Invoked from the example project via: npm run generate (or npx rive-toolkit-rn generate)
 */

function generate() {
  // Placeholder: add generation logic later
}

function run() {
  generate();
}

module.exports = { generate, run };
